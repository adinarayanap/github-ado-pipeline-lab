trigger:
  branches:
    include:
      - dev

pool:
  name: windows-selfhosted-pool

steps:
- script: |
    echo Pipeline triggered from GitHub dev branch
    echo Listing files:
    dir
  displayName: 'Run sample script (Windows)'

